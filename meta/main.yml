---

galaxy_info:
  author: Kostiantyn Nemchenko / Netways
  description: An Ansible role which installs and configures Patroni - HA solution for PostgreSQL
  namespace: netways
  company:
  license: MIT
  min_ansible_version: 2.4
  platforms:
  - name: EL
    versions:
    - 6
    - 7
  - name: Ubuntu
    versions:
    - xenial
    - bionic
  - name: Debian
    versions:
    - jessie
    - stretch
  galaxy_tags:
  - clustering
  - postgresql
  - patroni
  - availability
  - failover
  role_name: patroni
dependencies: []
#  - role: brianshumate.consul
#    when: patroni_dcs == "consul" and
#          not patroni_dcs_exists
#  - role: andrewrothstein.etcd-cluster
#    when: patroni_dcs == "etcd" and
#          not patroni_dcs_exists
#  - role: AnsibleShipyard.ansible-zookeeper
#    when: patroni_dcs == "zookeeper" and
#          not patroni_dcs_exists
